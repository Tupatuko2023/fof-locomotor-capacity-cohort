# Secure .Rprofile
# Do not fail if renv is not yet initialized

if (file.exists("renv/activate.R")) {
  source("renv/activate.R")
} else {
  message("renv/activate.R not found. renv is not active yet.")
}
